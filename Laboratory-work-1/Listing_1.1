Алгоритм Selection_sort (A[0..n-1])
// Сортування масиву методом вибору
// Вхідні дані: Масив A[0..n-1]
// Вихідні дані: Масив A [0..n-1], відсортований у неспадаючому порядку
for i = 0 to n-2 do
{
  min = i
  for j = i+1 to n-1 do
    if A[j] < A[min]
      min = j
  // Обмін A[i] та A[min]
  B = A[i]
  A[i] = A[min]
  A[min] = B
}
